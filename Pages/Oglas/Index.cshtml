@page
@model JobSeeker.Pages.Oglas.IndexModel
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
}
<div class="container-fluid text-left w-75 h-100 bg-light">
    <div class="row justify-content-xl-center">
        @*@foreach (var oglas in model)
        {
            @if (SignInManager.UserManager.GetUserId() == Model.UserID)
            {*@
                <div id="NaslovOglasa" class=" p-3 col-md-5">
                    <div class="">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">@*@*@oglas.NazivOglasa</h5>
                                <p class="card-text">@*@*@oglas.OpisOglasa</p>
                                <a class="btn btn-primary" asp-action="Index" asp-controller="Oglas" asp-route-ID="@*@*@oglas.ID">Više detalja</a>
                            </div>
                        </div>
                    </div>
                </div>
            @*}
        }*@
    </div>
</div>
@*}*@